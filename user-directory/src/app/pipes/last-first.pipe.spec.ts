import { LastFirstPipe } from './last-first.pipe';

describe('LastFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new LastFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
