import { VipHighlightDirective } from './vip-highlight.directive';

describe('VipHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new VipHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
